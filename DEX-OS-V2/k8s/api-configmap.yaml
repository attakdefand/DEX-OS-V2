apiVersion: v1
kind: ConfigMap
metadata:
  name: dex-api-config
data:
  SERVER_PORT: "3030"
  JWT_ISSUER: "dex-os-api"
  CORS_ALLOWED_ORIGINS: "http://localhost:8080"
  JWT_TTL_SECONDS: "900"
  JWT_MAX_TTL_SECONDS: "3600"
  WALLET_CHALLENGE_TTL_SECONDS: "300"
