[package]
name = "security_tests_runner"
version = "0.1.0"
edition = "2021"

[dependencies]
dex-core = { path = "../dex-core" }

[dev-dependencies]
tokio = { version = "1.0", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"